export default function ScedularLogo({ className = '' }: { className?: string }) {
  return <img src="/scedular_logo.png" alt="SCEDULAR timetable system" className={`block w-full max-w-[680px] h-auto ${className}`} />
}
