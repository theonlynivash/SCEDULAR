export default function ScedularLogo({ className = '' }: { className?: string }) {
  return <img src="/scedular-logo.svg" alt="SCEDULAR timetable system" className={`block w-full max-w-[680px] h-auto aspect-[1098/388] ${className}`} />
}
